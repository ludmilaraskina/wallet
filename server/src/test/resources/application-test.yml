spring:
  datasource:
    url: jdbc:h2:mem:/walletdb-test
    username: test
    password: test
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        ddl-auto: create
  flyway:
    enabled: false